# Ignore everything by default
*

# Allow necessary files
!dist/**
!package.json
!package-lock.json
!README.md
!LICENSE

# Webview assets
!src/webview/**

# oxc-parser native bindings (required at runtime, cannot be bundled by esbuild)
!node_modules/oxc-parser/**
!node_modules/@oxc-parser/**
