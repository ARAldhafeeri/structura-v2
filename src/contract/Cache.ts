/**
 * Simple contract for cache stats within structura simple recently modes, edges accessed cache implementation.
 */
export type CacheStats = { size: number; hits: number; misses: number; lastUpdated: Date };
