import * as assert from "assert";
// import * as vscode from "vscode";
// import * as sinon from "sinon";

suite("Couscous Extension Tests", () => {
    
  
    test("Test , testing setup", async () => {
        assert.equal(1,1);
    });
});